# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MovieList-CleanSwift' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Kingfisher'
  pod 'Cosmos'
  # Pods for MovieList-CleanSwift
 Pod 

  target 'MovieList-CleanSwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MovieList-CleanSwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
